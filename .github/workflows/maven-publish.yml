name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      # Đây là dòng quan trọng nhất: Phải khớp 100% với tên thư mục trên GitHub của bạn
      run: mvn -B package --file BAI03-W9/pom.xml

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: BankSystem-Jar
        path: BAI03-W9/target/*.jar
